# 5 DDR5 MC与DDR6 MC差异及影响分析

## 5.1 分析目的与范围

本章基于现有DDR5 MC架构及当前DDR6 MC设计方案，从Memory Controller实现角度分析DDR6设计对现有MC架构、功能模块及状态管理方式产生的影响，并确定后续设计中的继承、修改、重构及新增范围。

本章重点分析与MC设计直接相关的地址及资源组织、事务管理、Bank状态、Timing、命令调度、Refresh/RFM、Read/Write数据通路、配置管理以及MC与PHY接口等内容。

本章不对DDR5和DDR6器件标准进行完整条款级差异对比。对于尚需DDR6器件规范、产品Topology或PHY接口控制文件进一步确认的内容，本章仅说明其对MC可能形成的设计约束，不预设具体参数或物理实现。

## 5.2 总体差异及设计影响

现有DDR5 MC已经形成Host请求接收、地址处理、事务存储、命令构造、调度、Bank状态管理、Timing控制、Read/Write数据处理及维护类服务等较完整的功能架构。当前DDR6 MC设计不改变上述基本分层，而主要针对DDR6协议及目标平台引入的资源组织、命令Timing、维护机制、接口关系和执行状态边界进行演进。

从设计影响角度，现有DDR5 MC向DDR6 MC的主要变化见表1。

**表1 DDR5 MC向DDR6 MC演进的主要设计影响**

| 序号 | 变化类别                  | 现有DDR5设计基础                        | DDR6目标设计处理                                                             | 主要影响模块                   |
| -- | --------------------- | --------------------------------- | ---------------------------------------------------------------------- | ------------------------ |
| 1  | 请求及事务身份               | 以现有token、entry index及局部关联关系为主     | 建立Parent/Child、Attempt、Slot/Generation及Session等明确身份边界                  | HIF、TE、Read、Write、PI     |
| 2  | 地址及资源组织               | 现有地址字段提取和Bank/Page相关表示            | 形成System Physical Address到canonical resource的统一映射，并由Geometry统一定义资源合法性  | IH、Geometry、TE           |
| 3  | 事务生命周期                | 部分请求可随RD/WR命令发出而删除                | 请求持续至逻辑事务达到terminal条件，命令执行仅作为事务进展                                      | TE、HIF、Read、Write        |
| 4  | Bank及OpenRow状态        | Bank状态、page-open及部分Timing信息存在分散保存 | BSM、OpenRow和Timing分别维护唯一职责范围内的状态                                       | BSM、OpenRow、Timing       |
| 5  | Timing管理              | 固定counter及局部Timing状态较多            | 采用统一Timing查询、History及Reservation机制，并根据actual event更新                   | Timing、CC、GS             |
| 6  | 命令调度                  | 保留候选生成及调度基本结构                     | 明确Selection、Admission、Presentation和Actual边界，增加资源原子预留和freshness检查       | CC、OS、GS、PI              |
| 7  | PHY接口                 | 沿用现有接口及流水实现                       | MC与PI/PHY采用1:8 Simplified DFI，并增加DDR6 command encoding及phase placement | PI、Command Issue         |
| 8  | Refresh               | 保留周期、debt及调度基本机制                  | Refresh目标模型由原有REFsb相关处理演进为REF_AB/REF_DIST模型                            | Timing、Refresh、Geometry  |
| 9  | RFM及激活管理              | 已存在独立RFMC及per-bank激活计数基础          | 保持Refresh与RFM独立，引入ACT lifetime以及PRAC/BAT/PRFM相关计账语义                    | RFMC、BSM、Timing          |
| 10 | Read返回                | 保留Read tracking及数据处理通路            | 增加Expected、Assembly、精确arm及generation匹配机制                               | RT、RD、PI、HIF             |
| 11 | Write数据               | 保留WDATA RAM及数据捕获结构                | Payload与Attempt解耦，采用稳定Payload Handle，并分离Result、Release和Reclaim         | HIF、WU/WDATA、PI          |
| 12 | 配置管理                  | 软件配置可较直接作用于运行逻辑                   | 区分Candidate与Active配置，在安全点统一发布Config、Geometry及相关Timing视图                | Config、Geometry、Timing   |
| 13 | MR/FSP/Power/Training | 保留已有控制及序列框架                       | 补充Programmed/Effective状态及明确完成边界                                        | CI、MR、FSP、Power、Training |

上述变化表明，DDR6 MC的主要工作不是重新建立一套新的控制器架构，而是在保留现有模块职责及数据通路的基础上，对原有DDR5设计中依赖隐式状态、局部Timing、简单身份关联及提前释放等实现方式进行调整，使各模块能够适配DDR6下更明确的资源、Timing和执行生命周期管理。

## 5.3 地址及资源组织变化

### 5.3.1 地址处理职责变化

现有DDR5 MC地址处理结构可继续保留输入流水、地址字段处理及IH至TE的请求传递机制，但地址映射的功能语义需要根据DDR6目标资源组织重新定义。

DDR6 MC中，Normal MC仍以System Physical Address作为上游地址输入。IH首先完成直接位级地址映射，获得Channel、Logical Bank、Row及Column等逻辑字段，再依据当前Geometry配置转换为统一的canonical resource及目标资源描述。

因此，地址解析不再仅表示从地址中提取固定Bank、Row或Column字段，还应完成以下处理：

a) 根据活动配置选择对应地址映射关系；

b) 根据Geometry确定目标资源组织；

c) 对器件、Bank、Row及Column等字段进行完整范围检查；

d) 形成后续TE、BSM、OpenRow、Timing及Scheduler共同使用的统一资源表示；

e) 对非法地址形成明确的Reject结果，不采用默认置零或静默重映射方式继续执行。

### 5.3.2 Geometry统一资源定义

DDR6 MC通过Geometry统一定义指定Profile、Density及Topology下的资源组织和合法范围。

Geometry不替代IH完成Host地址译码，也不负责保存当前Bank状态或OpenRow状态。其主要作用是向地址解析及其他MC模块提供一致的资源定义，使相同逻辑资源在事务存储、Bank状态、Timing、Refresh及维护类操作中采用一致的canonical identity。

因此，DDR5设计中可保留地址提取和流水实现形式，但资源映射的功能语义应由Geometry统一管理，不应在不同模块分别形成独立的资源解释。

## 5.4 请求身份及事务生命周期变化

### 5.4.1 请求身份管理

现有DDR5设计中，部分模块可以依据当前slot、token、phase或请求到达顺序完成局部关联。DDR6目标设计中，请求可能经历较长时间的stall、retry及late feedback，同一物理slot还可能在旧反馈到达前重新分配。

因此，DDR6 MC应建立更加明确的事务身份管理机制。

HIF在请求进入IH之前建立Transaction及Parent/Child逻辑身份。对于可能重复使用的有限表项，应采用slot和generation共同标识一次具体分配。发生retry时，应保持Parent/Child逻辑身份不变，并使用新的attempt区分不同执行尝试。

各模块接收异步返回或完成事件时，应依据对应对象定义的完整identity进行匹配。仅slot/index相同而generation、attempt或session不一致的事件，不得更新当前有效记录。

### 5.4.2 Transaction Terminal与资源回收

现有DDR5 TE中部分Read或Write请求存在随RD/WR命令发出而删除entry的实现方式。DDR6目标设计不继续采用“命令发出即事务结束”的语义。

一条Read事务可能依次经历PRE、ACT、RD、数据返回及结果确认；一条Write事务在WR命令发出后，还可能继续等待Write Data物理接收、drain、final status或retry处理。因此：

a) PRE和ACT属于事务中间进展，不终止TE请求；

b) RD或WR被选择、准入或提交均不直接构成transaction terminal；

c) Read应依据最终Read结果及对应child terminal条件结束；

d) Write应依据Write Manager形成的最终terminal disposition及retry状态结束；

e) 事务达到terminal后，还应等待外部引用全部解除，方可完成slot reclaim。

由此，DDR6设计应将命令进展、逻辑完成及物理资源回收分别管理。

## 5.5 Bank状态、OpenRow及Timing管理变化

### 5.5.1 状态职责分离

现有DDR5设计中，Bank状态、page-open信息及部分Timing计数可能分别存在于Bank状态机、TE entry或其他局部逻辑中。为避免同一DRAM事实存在多个可独立演进的状态副本，DDR6目标设计对相关职责进行进一步划分。

BSM只维护Bank当前所处的访问状态，并回答当前状态是否允许指定类型操作。

OpenRow作为当前打开Row信息的唯一功能真值，负责保存valid、row及必要的Geometry上下文。OpenRow只根据实际ACT事件建立打开行，并根据有效PRE、Auto Precharge或其他qualified close事件清除打开行。

Timing负责命令之间的协议时间约束，不由BSM或TE中的局部counter形成第二套Timing真值。

TE等模块可以缓存Bank或OpenRow查询结果以改善实现性能，但缓存结果必须具备freshness判断能力，不得作为独立状态源继续接收ACT/PRE事件并自行演化。

### 5.5.2 Timing表示方式变化

DDR6目标设计保留现有Timing控制的基本功能，但逐步将固定Read/Write gap counter及分散的per-bank Timing状态演进为统一的Timing History、规则查询和Reservation机制。

对于tRAS、tRP、tRC、tRCD、tRRD、tFAW、tCCD、tWTR、tRTW及Refresh相关Timing，应由Timing模块根据目标资源关系、实际发生事件及当前有效Timing配置统一判断。

其中，涉及Bank、Bank Group、PSD或Shared DQS Group等资源关系的Timing条件，应建立在canonical resource关系基础上。

Timing状态原则上由经过确认的actual event更新。Scheduler grant、GS admission或PI presentation均不得直接替代实际DRAM事件写入Timing History。

## 5.6 命令构造、选择及准入机制变化

现有DDR5 MC的Command Constructor、Ordinary Scheduler及最终调度/准入结构具备继续使用的基础，因此DDR6设计继续保持CC、OS和GS的分层关系。

CC根据当前TE请求、BSM状态、OpenRow及Timing查询结果确定下一条需要执行的DRAM命令。

OS继续负责普通Host候选之间的选择，并可保留现有priority、QoS、aging等性能策略。

DDR6目标设计重点增强GS的最终准入职责。在候选命令真正成为控制器执行承诺前，GS还应检查：

a) Bank及OpenRow查询结果是否仍然有效；

b) Timing条件是否满足；

c) 当前配置及Geometry上下文是否匹配；

d) 所需Read、Write、PI及event资源是否具备足够容量；

e) 相关资源是否已经被其他已准入但尚未执行的命令预留；

f) Refresh、RFM等维护类服务是否形成更高优先级的阻塞或deadline约束。

因此，OS选择一个候选仅表示选择结果，不能等同于资源已经占用。只有GS完成最终条件检查并建立相应Reservation后，该命令才进入Admission状态。

## 5.7 Refresh、RFM及激活管理变化

### 5.7.1 Refresh

现有DDR5 PAS_RC中的周期计时、debt、selector及仲裁等设计思想原则上继续保留。

DDR6目标方案将原有与REFsb相关的处理演进为REF_AB和REF_DIST两类控制语义。其中REF_DIST的具体目标集合由Geometry根据当前器件组织进行展开。

Refresh Timing负责Refresh的时间calendar、deadline、tRFC及目标集合相关Timing限制；Refresh Controller负责Refresh obligation、debt、service progression、selector及coverage。二者通过明确接口关联，不重复维护同一状态。

### 5.7.2 RFM、PRAC及ACT Lifetime

现有设计中RFM Controller与普通Refresh Controller已经相互独立，该结构在DDR6中继续保留。

DDR6目标设计进一步区分ACT identity、PRAC/BAT计账及RFM service三个层次。

ACT首先建立独立的Activation Lifetime。PRAC、BAT及PRFM等相关机制在该ACT生命周期基础上完成计账和触发条件判断。达到规定条件后，再由RFM模块建立独立的maintenance service。

普通Refresh和RFM可以共享后端PRE准备、Maintenance Arbitration、GS、PI和Timing等资源，但不共用前端的debt、credit、coverage或状态机。

因此，Refresh与RFM应保持独立的功能owner关系，避免将两类不同维护义务合并为同一计数或credit体系。

## 5.8 Read及Write数据通路变化

### 5.8.1 Read数据通路

现有Read Tracking、数据保护及数据处理通路原则上继续复用。

DDR6目标设计重点加强Read请求与物理返回之间的精确关联。Read命令准入时，应提前确认Expected、Assembly及返回入口等资源可用，并建立相应Reservation。

Read返回在成为可匹配对象前，应经过明确的arm过程。只有返回数据携带或恢复出的完整逻辑identity与当前Expected记录一致，且记录已进入可匹配状态时，方可对Read Assembly产生功能性写入。

Read数据和最终状态完成后形成Host结果，但Host结果传输与内部Expected、Assembly及PI关联资源的回收仍属于不同事件。

### 5.8.2 Write数据通路

现有DDR5 WDATA RAM、数据捕获及stall控制结构原则上继续保留，但Write数据的identity和生命周期需要调整。

DDR6 Write采用稳定Payload Handle关联Host Write请求与WDATA。Payload Handle由slot及generation构成，Payload allocation与首个WDATA capture应采用明确的状态边界。

Payload完成捕获后保持不变，各执行attempt仅引用同一份Payload，不因retry重新从Host获取Write Data。

Write命令与Payload在PI侧依据完整identity进行关联，不使用“最近一条Write”或隐式顺序完成匹配。

Write执行完成后，应区分：

a) Write result形成；

b) Result向上级传输；

c) Attempt Reservation释放；

d) Payload不再被任何对象引用；

e) Payload资源最终回收。

上述条件不应合并为单一“Write完成”事件。

## 5.9 配置、模式及PHY接口变化

### 5.9.1 配置管理

现有DDR5 APB地址译码、CSR传输、APB/Core CDC及软件寄存器组织方式原则上可以继续使用。

DDR6目标设计不继续采用“软件写入配置后立即作为全局活动配置使用”的隐含假设。

软件写入的新配置首先形成Candidate Config，经合法性检查以及Geometry、Timing等相关模块准备后，仅在满足全局安全切换条件时，方可统一发布为新的Active Config。

已经进入MC的旧事务应继续使用其对应的有效配置上下文，不得在配置切换后使用新Geometry重新解释旧事务。

### 5.9.2 MR、FSP、Power及Training

现有Mode Register、Power及相关控制序列框架原则上继续保留。

DDR6目标设计重点补充Programmed状态与Effective状态之间的区分，并明确MR、FSP、Power状态切换以及Training操作真正完成的判定边界。

对于依赖PHY物理结果的Training、FSP切换及相关状态，不得仅依据MC已发出请求推断其已经生效。

### 5.9.3 MC与PHY接口

DDR6目标设计中，MC与PI/PHY之间固定采用1:8 Simplified DFI逻辑接口。

PI继续保留MC与PHY之间的寄存及流水边界，同时增加DDR6 Command Encoder及8-phase Command Packer等功能，将已经由GS确定的命令及phase位置转换为Simplified DFI接口记录。

PI还应负责管理命令从Admission到Presentation以及PHY物理反馈之间的关联关系。

PHY侧accept、launch、actual、Read return、Write drain及final status等精确反馈载体仍需由正式PHY接口控制文件定义。在相关接口未闭合前，MC应保留对应逻辑状态及设计门禁，不自行假定物理延迟或返回编码。

## 5.10 模块演进关系

基于上述分析，DDR5 MC现有模块的总体演进关系可归纳如下：

a) HIF、Host Port Arbiter及CDC结构以继承为主，重点增强事务身份、Parent/Child和Host完成边界；

b) IH和TE保留请求流水及有界entry结构，重点修改地址资源语义、Transaction生命周期和Dependency管理；

c) CC、OS和GS保持现有分层，重点增加查询freshness、Reservation及维护类服务统一准入能力；

d) BSM保留per-bank状态管理思想，将Timing真值从Bank本地状态中分离；

e) OpenRow保留Row/Valid存储结构，并以actual ACT及qualified close作为唯一功能更新条件；

f) Timing保留协议Timing判断职责，并演进为统一的History、Query及Reservation机制；

g) Refresh继续沿用PAS_RC主要结构，并将目标模型演进为REF_AB及REF_DIST；

h) RFM继续保持独立控制链，并增加ACT Lifetime及PRAC/BAT/PRFM相关语义；

i) Read继续复用Tracking和Datapath，在其上增加Expected、Assembly、Arm及Generation关联机制；

j) Write继续复用Payload RAM和捕获通路，将Payload与Attempt解耦，并明确Result、Release及Reclaim边界；

k) Config继续复用寄存器访问及CDC框架，增加Candidate、Prepare、Safe Commit及版本管理机制；

l) MR、FSP、Power及Training继续利用原有序列控制基础，并补充状态真正生效及完成的判断边界；

m) PI继续作为MC与PHY之间的接口边界，并按照1:8 Simplified DFI要求完成DDR6命令编码、phase组织及反馈管理。

## 5.11 待评审事项

根据当前设计材料，下列内容尚未完全闭合，需要在后续专家评审、产品规格确认或PHY接口定义过程中进一步确定：

a) Product Topology及部分器件组织的最终定义，包括1p12相关映射；

b) PRAC、BAT、PRFM等功能的具体产品参数及部分计账规则；

c) 部分Timing参数、Shared DQS关系及最终时间换算方式；

d) PHY侧accept、launch、actual、Read return、Write drain及final status的精确定义和载体；

e) 1:8 Simplified DFI下跨接口周期命令及数据的具体连续承载方式；

f) Read Expected、Assembly、PI Join及其他有限资源的最终深度和物理端口数量；

g) MR、FSP及Training的部分状态authority和完成条件；

h) Config、Geometry及Timing统一发布过程中的部分接口细节；

i) CDC/RDC、Reset及no-old-return等跨边界证明条件。

上述待确认事项不改变当前DDR6 MC总体架构及主要模块职责。后续设计应在相关输入闭合后补充具体参数、接口及状态条件，并通过详细设计和验证章节完成追踪。
